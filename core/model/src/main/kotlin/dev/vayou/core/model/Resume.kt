package dev.vayou.core.model

/** Whether a film reopens where it was left. */
enum class Resume {
    /** Where it was left, which is what a film watched over two evenings needs. */
    YES,

    /** From the start, every time. */
    NO,
}
